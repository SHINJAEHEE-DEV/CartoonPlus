# 카툰플러스 기술 도입 의사결정서

**상태:** 승인된 MVP 기준  
**마지막 검토:** 2026-09-14
**적용 범위:** 고객용 웹, 직원용 관리 화면, 빌드·배포, 개발·문서 관리 방식

## 1. 의사결정의 기준

카툰플러스는 만화카페 고객의 빠른 도서 탐색과 매장 직원의 재고·입고 신청·안내 방송 업무를 지원한다. MVP의 기술 선택은 다음 기준을 우선한다.

1. 월 고정 운영비를 0원으로 유지한다.
2. 모바일 고객 화면과 데스크톱 POS 화면을 빠르게 개선한다.
3. 도서 재고와 입고 신청을 지점별로 정확하게 관리할 수 있어야 한다.
4. 한글 공백 무시·초성 검색과 한국어 TTS를 브라우저에서 가볍게 제공한다.
5. MVP 검증 후 실제 데이터·인증 기반으로 확장할 수 있어야 한다.

## 2. 기술 선택 요약

| 영역 | 선택 | 상태 | 선택 이유 |
| --- | --- | --- | --- |
| 배포 | Cloudflare Pages + GitHub 직접 연동 | Pages 배포·자동 재배포 검증 완료 | main 변경을 자동 배포하며 검증 기간에는 기존 GitHub Pages를 유지한다. |
| 프레임워크 | React | 적용 | 고객·직원 화면의 상태와 재사용 UI를 관리한다. |
| 개발 서버·빌드 | Vite | 적용 | 빠른 개발 환경과 정적 번들 생성을 제공한다. |
| 언어 | TypeScript | 적용 | 재고, 입고 신청, 지점 데이터의 구조 오류를 줄인다. |
| 스타일 | CSS (`src/styles.css`) | 적용 | 현재 별도 CSS로 반응형 화면과 브랜드 디자인을 구현한다. |
| 화면 이동 | React Router | 적용 | 고객 화면과 보호된 직원 화면을 SPA에서 분리한다. |
| 테스트 | Vitest, Testing Library | 적용 | 검색 같은 핵심 로직과 사용자 흐름을 빠르게 검증한다. |
| 데이터·인증 | Supabase | 적용 | 실제 지점 데이터, 입고 신청, 직원 인증을 중앙 관리한다. |
| 개발 협업 | Codex | 적용 | 기획·명세·구현·검증 기록의 연결을 강화한다. |

## 3. 배포: Cloudflare Pages와 GitHub 직접 연동

### 선택 이유와 전환 범위

2026-09-11 사용자 승인: GitHub 직접 연동 방식으로 Cloudflare Pages로 이전한다. Actions에서 Cloudflare 배포 토큰을 별도로 관리하지 않고 `main` 변경을 자동 배포한다. Supabase 데이터·인증은 유지하며, 도메인 구매·연결은 대표 승인 후 마지막 작업으로 진행한다. 우선 `pages.dev` 주소에서 검증한다.

### 배포 설정

| 항목 | 값 |
| --- | --- |
| 저장소 | `SHINJAEHEE-DEV/CartoonPlus` |
| Production branch | `main` |
| Root directory | 저장소 루트 |
| Build command | `npm run build:cloudflare` |
| Build output directory | `dist` |
| Node 환경변수 | `NODE_VERSION=22.16.0` |
| Supabase 공개 설정 | `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` |

Supabase 공개 설정은 Cloudflare 빌드 환경에 등록 완료했다. 생성 화면에서 Production·Preview 양쪽에 적용되었으며 현재는 기존 운영 프로젝트를 연결한다. GitHub Secrets는 자동 이전되지 않는다. 서비스 역할 키는 브라우저 빌드에 넣지 않는다. Preview에서 운영 데이터를 수정하는 검증을 하지 않으며, 별도 테스트 데이터가 필요하면 Preview 연결을 분리한다.

`npm run build`와 `npm run build:cloudflare`는 모두 Cloudflare Pages 도메인 루트(`/`)를 기준으로 빌드한다. GitHub는 소스 저장소와 Cloudflare Pages의 자동 배포 연결에만 사용하며, GitHub Pages는 운영 배포 대상이 아니다. 앱은 React Router의 BrowserRouter와 빌드 시 정적 HTML 생성(SSG)을 사용한다. 직접 경로와 정적 자산의 호환성은 Cloudflare 배포에서 별도 검증한다.

현재 서비스 주소: https://cartoonplus.pages.dev (첫 배포 커밋 `560d921`).

### 완료 기준과 복구

- Cloudflare 빌드 성공과 발급된 HTTPS 주소 접속 확인.
- 홈, 도서 검색, 메뉴, 직원 로그인 화면 및 새로고침 확인.
- 이미지·오디오·CSV가 올바른 경로에서 제공되는지 확인.
- 후속 main 커밋 `6cc00b6` 자동 재배포 success 확인(25초).
- 검증 완료 전 기존 사이트 주소를 유지한다. 새 Cloudflare 배포에 문제가 있으면 기존 주소를 계속 사용한다.

직원 로그인 성공 여부는 배포 후 확인하고, 인증 오류가 남으면 별도 인증 정상화 작업으로 이어간다. 공식 도메인, 포털 링크 교체와 유료 서비스 해지는 이번 작업에 포함하지 않는다. 무료 플랜 범위 내 운영을 목표로 하며 비용 절감 확정은 기존 유료 구독 해지 후 판정한다.

공식 참고: https://developers.cloudflare.com/pages/framework-guides/deploy-a-react-site/

## 4. 프론트엔드 기술 스택

### React

React는 지점 선택, 검색어·장르 필터, 도서 입고 신청 모달, 로그인 상태, 방송 재생 상태처럼 화면의 상태가 많은 서비스를 관리하기 위해 선택했다. 같은 도서 카드, 헤더, 모달, 관리자 표를 컴포넌트로 분리하면 고객 화면과 직원 화면의 수정 범위를 줄일 수 있다.

또한 Supabase 연동 시 데이터 조회와 갱신 결과를 화면 상태에 연결하기 쉬워 MVP의 Mock 데이터 구조에서 실제 데이터 구조로 전환하기에 적합하다.

### Vite

Vite는 React 프로젝트의 개발 서버와 프로덕션 번들을 담당한다. 개발 중 화면·스타일 변경을 빠르게 확인할 수 있고, 결과물을 정적 파일로 생성하므로 GitHub Pages 배포 방식과 바로 연결된다.

Next.js처럼 서버 렌더링 중심의 프레임워크는 현재 정적 호스팅과 브라우저 기반 검색·TTS 요구에 비해 운영 구조가 무거워 MVP에서 채택하지 않았다. 서버 렌더링, SEO 중심 콘텐츠 운영, 서버 API가 제품의 핵심이 되면 재검토한다.

### TypeScript

TypeScript는 `Book`, `BookInventory`, `BookRequest`, `Store`처럼 서로 연결된 데이터를 다룰 때 필드 누락과 타입 불일치를 빌드 전에 발견하기 위해 사용한다. 특히 도서명·권수·서가 위치를 분리하고 지점별 재고를 연결하는 데이터 모델에서 오류를 줄인다.

### 스타일과 UI 보조

현재 스타일은 `src/styles.css`와 컴포넌트 인라인 스타일로 구현한다. Tailwind CSS, Lucide React, `clsx`, `tailwind-merge`는 현재 의존성에 없으므로 적용 기술로 표기하지 않는다. 브랜드 토큰과 반응형 규칙을 더 체계화할 필요가 생기면 후보를 비교한 뒤 별도 결정한다.

### React Router

React Router는 홈, 검색, 즐길거리, 메뉴, 매장 안내, 직원 콘솔을 각 경로로 나누고 URL 기반 탐색을 제공한다. 직원 콘솔은 인증 상태에 따라 접근을 제어하는 경계가 필요하므로 별도 경로로 관리한다.

### Vitest와 Testing Library

Vitest는 Vite 프로젝트와 같은 도구 체계를 사용해 빠르게 테스트를 실행한다. Testing Library는 사용자가 보는 결과를 기준으로 화면 흐름을 검증하는 데 사용한다. 우선 검증 대상은 공백 무시·초성 검색, CSV/XLSX 가져오기, 인증 경계, TTS 상태 변화다.

## 5. Supabase: 실제 운영 데이터와 인증

### 도입 이유

Supabase는 현재 고객 도서 카탈로그, 도서 신청, 직원 인증과 운영 데이터를 제공한다. 환경 변수가 없을 때만 로컬 CSV 카탈로그로 제한적으로 폴백한다.

Supabase는 PostgreSQL 데이터베이스, 인증, 행 단위 보안 정책을 제공하므로 다음 문제를 하나의 관리 지점에서 해결할 수 있다.

- `stores`, `books`, `book_inventories`, `book_requests`를 관계형 데이터로 관리한다.
- 지점별 재고와 입고 신청을 여러 기기에서 동일하게 조회·수정한다.
- STAFF와 ADMIN 권한에 따라 재고 수정, 신청 처리, 운영 설정 접근을 구분한다.
- 프론트엔드에서 사용할 수 있는 JavaScript 클라이언트를 제공해 React 구조와 자연스럽게 연결한다.
- 초기 데이터 규모와 MVP 트래픽에 무료 티어가 적합해 운영비 0원 목표를 유지한다.

### 대안과 판단

| 대안 | 판단 |
| --- | --- |
| `localStorage` 유지 | 단일 브라우저 검증에는 적합하지만 다기기 데이터 공유와 실제 운영에 부적합하다. |
| 자체 API + 별도 PostgreSQL | 유연성은 높지만 서버·배포·보안 운영 비용이 MVP에 과하다. |
| Firebase | 실시간 문서형 데이터에는 강점이 있으나, 지점·도서·재고·입고 신청의 관계와 SQL 조회 요구에는 PostgreSQL 모델이 더 자연스럽다. |

### 도입 범위와 전제

`docs/DATA_MODEL.md`를 기준으로 `(store_id, book_id)` 중복 방지, 보관, 입고 신청 상태, STAFF/ADMIN 권한을 운영한다. 현재 CSV 업로드와 공개 카탈로그는 `snu` 지점으로 고정되어 있으므로, 두 추가 지점 반영 전에 지점 선택 구현과 검증이 필요하다.

## 6. Codex 에이전트 사용

Codex는 제품 런타임에 포함되는 기술이 아니라 프로젝트 운영 도구다. 이 세션에서는 10년차 시니어 PM 관점에서 기획서, 요구사항 명세서, 데이터 모델, 기능 명세, 설정 현황, 트러블슈팅 기록을 연결한다.

### 사용하는 이유

- 요구사항, ADR, 코드, 테스트, 이슈 티켓 사이의 불일치를 조기에 찾는다.
- 기술 도입과 로직 선택의 근거·대안·영향을 문서로 남긴다.
- 구현 전 완료 기준을 구체화하고, 구현 후 검증 결과와 남은 위험을 기록한다.
- 반복되는 장애 원인과 해결 방법을 `docs/TROUBLESHOOTING.md`에 축적한다.

### 사람과 Codex의 역할

| 역할 | 책임 |
| --- | --- |
| 제품 책임자 | 제품 목표, 우선순위, 예산, 정책, 기술 선택의 최종 결정을 내린다. |
| Codex | 현황 조사, 선택지와 근거 정리, 문서 초안, 영향 분석, 검증과 기록을 수행한다. |

기술·로직·데이터 모델의 선택이 필요한 작업에서는 Codex가 후보, 근거, 비용과 영향, 권장안을 먼저 제시하고 제품 책임자와 문답으로 결정한다. 결정된 내용은 이 문서 또는 관련 ADR에 기록한다.

## 7. 연관 문서와 갱신 규칙

| 문서 | 역할 |
| --- | --- |
| `docs/REQUIREMENTS.md` | 기능·비기능 요구사항과 우선순위의 기준 |
| `docs/DATA_MODEL.md` | 적용 중인 Supabase 데이터 모델과 제약 |
| `docs/adr/0001-frontend-stack-and-mock-first.md` | 프론트엔드 스택과 Mock-first 초기 결정 |
| `docs/adr/0002-core-algorithms-auth-routing.md` | 검색, 인증, 라우팅 결정 |
| `docs/TROUBLESHOOTING.md` | 실제 문제와 해결 기록 |

새 기술을 도입하거나 기존 기술을 교체할 때는 이 문서에 선택 이유와 전환 조건을 반영하고, 결정의 세부 사항은 새 ADR로 남긴다.
