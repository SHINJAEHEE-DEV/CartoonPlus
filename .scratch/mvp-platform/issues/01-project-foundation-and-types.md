# 01: Project Foundation, Core Types, Mock Store & Theme

**What to build:** Set up the Vite + React + TypeScript application with Tailwind CSS configured with the CartoonPlus signature color palette (Yellow `#FED943`, Charcoal `#1E1E1E`). Define domain TypeScript interfaces for Store, Book, BookInventory, BookRequest, EntertainmentItem, MenuItem, BroadcastPreset, and AdminUser, along with realistic initial seed data for Seoul National Univ. Station and Jamsil stores.

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] Vite + React + TypeScript + Tailwind CSS project initialized and building successfully
- [x] Domain interfaces for Store, Book, BookInventory, BookRequest, EntertainmentItem, MenuItem, BroadcastPreset defined matching data model
- [x] Initial mock seed data for 'snu' and 'jamsil' stores configured (603 real books parsed from CSV)
- [x] Local storage persistence helper for store selection and mock state changes
