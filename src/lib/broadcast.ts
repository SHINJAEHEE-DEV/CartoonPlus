export function speakKorean(text:string):Promise<void>{return new Promise((resolve,reject)=>{if(!('speechSynthesis'in window))return reject(new Error('이 브라우저는 방송을 지원하지 않습니다.'));const utterance=new SpeechSynthesisUtterance(text);utterance.lang='ko-KR';utterance.onend=()=>resolve();utterance.onerror=()=>reject(new Error('방송 재생에 실패했습니다.'));window.speechSynthesis.cancel();window.speechSynthesis.speak(utterance);});}
