import {describe,expect,it}from'vitest';import{isDue}from'./broadcastSchedule';describe('isDue',()=>{it('handles daily weekday and once schedules',()=>{const now=new Date('2026-09-08T14:00:00');expect(isDue({scheduleType:'daily',targetTime:'14:00',isEnabled:true},now)).toBe(true);expect(isDue({scheduleType:'once',targetTime:'14:00',targetDate:'2026-09-08',isEnabled:true},now)).toBe(true);expect(isDue({scheduleType:'daily',targetTime:'14:00',isEnabled:false},now)).toBe(false)})});
