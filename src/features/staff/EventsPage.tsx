import { useState } from 'react';import { supabase } from '../../lib/supabase';
export function EventsPage(){const [message,setMessage]=useState('');const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const {data:store}=await supabase!.from('stores').select('id').eq('slug','snu').single();const {error}=await supabase!.from('store_events').insert({store_id:store?.id,title:String(f.get('title')),content:String(f.get('content')),start_date:String(f.get('start')),end_date:String(f.get('end')),is_public:true});setMessage(error?.message??'이벤트를 저장했습니다.');};return <main className="app-notice"><h1>이벤트 관리</h1><form onSubmit={save}><input name="title" placeholder="이벤트 제목" required/><textarea name="content" placeholder="내용" required/><input name="start" type="date" required/><input name="end" type="date" required/><button>공개 저장</button></form><p>{message}</p></main>}
