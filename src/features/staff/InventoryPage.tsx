import { useState } from 'react';
import { parseBaselineInventory } from '../../lib/inventoryCsv';
import { supabase } from '../../lib/supabase';
export function InventoryPage(){const [message,setMessage]=useState('');const save=async(form:FormData)=>{const {error}=await supabase!.rpc('upsert_inventory',{p_title:String(form.get('title')),p_author:String(form.get('author')),p_category:String(form.get('category')),p_volume_range:String(form.get('volume')),p_shelf_location:String(form.get('shelf'))});setMessage(error?.message??'재고를 저장했습니다.');};const csv=async(file:File)=>{const books=parseBaselineInventory(await file.text());let done=0;for(const book of books){const {error}=await supabase!.rpc('upsert_inventory',{p_title:book.title,p_author:book.author,p_category:book.category,p_volume_range:book.volumeRange||'확인 중',p_shelf_location:book.shelfLocation});if(error){setMessage(error.message);return;}done+=1;}setMessage(`${done}개 재고를 추가하거나 갱신했습니다. 기존에만 있던 재고는 유지됩니다.`)};return <main className="app-notice"><h1>도서 재고 관리</h1><form onSubmit={e=>{e.preventDefault();void save(new FormData(e.currentTarget))}}><input name="title" placeholder="도서명" required/><input name="author" placeholder="작가"/><input name="category" placeholder="장르"/><input name="volume" placeholder="보유 권수 (예: 1~12권)" required/><input name="shelf" placeholder="서가 위치 (예: 책장 7번)" required/><button>저장</button></form><label>CSV 가져오기 <input type="file" accept=".csv,text/csv" onChange={e=>{const file=e.target.files?.[0];if(file)void csv(file)}}/></label><p>{message}</p></main>}
