import { useState } from 'react';import { supabase } from '../../lib/supabase';
export function GamesPage(){const [message,setMessage]=useState('');const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const {data:store}=await supabase!.from('stores').select('id').eq('slug','snu').single();const {error}=await supabase!.from('entertainment_items').insert({store_id:store?.id,item_type:String(f.get('type')),title:String(f.get('title')),players:String(f.get('players')),genre:String(f.get('genre')),is_verified:true,is_available:true});setMessage(error?.message??'검증 게임을 저장했습니다.');};return <main className="app-notice"><h1>게임 목록 관리</h1><form onSubmit={save}><input name="type" placeholder="종류" required/><input name="title" placeholder="게임 제목" required/><input name="players" placeholder="인원"/><input name="genre" placeholder="장르"/><button>검증 후 공개</button></form><p>{message}</p></main>}
