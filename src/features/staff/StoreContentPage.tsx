import { useState } from 'react';import { supabase } from '../../lib/supabase';
export function StoreContentPage(){const [message,setMessage]=useState('');const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const {data:store}=await supabase!.from('stores').select('id').eq('slug','snu').single();const {error}=await supabase!.from('store_content').upsert({store_id:store?.id,content_key:String(f.get('key')),content_value:{text:String(f.get('text'))}},{onConflict:'store_id,content_key'});setMessage(error?.message??'공개 정보를 저장했습니다.');};return <main className="app-notice"><h1>매장 공개 정보 관리</h1><form onSubmit={save}><input name="key" placeholder="구분 (예: hours, pricing)" required/><textarea name="text" placeholder="공개할 내용" required/><button>저장</button></form><p>{message}</p></main>}
